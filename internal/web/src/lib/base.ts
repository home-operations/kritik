// basePath is the URL prefix the UI (and its API calls) are served under.
// The app is embedded at the web server's root, so this is always empty for
// now; it exists as a single seam if that ever changes.
export const basePath = '';
