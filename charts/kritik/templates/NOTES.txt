kritik {{ .Chart.AppVersion }} is installed as {{ include "kritik.fullname" . }} with roles: {{ include "kritik.enabledRoles" . }}.

{{ if include "kritik.hasIngest" . -}}
Point each installation's webhook at:

  https://<your host>/hooks/<installation name>

with the installation's webhook secret, content type application/json, and the
pull_request, issue_comment, pull_request_review_comment, push and installation
events. Inside the cluster the listener is
{{ include "kritik.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}.
{{- end }}

{{ if include "kritik.hasWorker" . -}}
Runner Jobs appear in this namespace as kritik-run-*; every run is also recorded
in the runner_runs table.
{{- end }}

{{ if not (include "kritik.embeddingEnabled" .) -}}
Indexing is off (embedding.model is empty): reviews run without the similar-code
context stage.
{{- end }}

Health: /healthz and /readyz, metrics: /metrics, all on port {{ .Values.service.metricsPort }}
of every pod ({{ include "kritik.fullname" . }}-metrics Service).
